{
  "name": "tasktracker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Персональный трекер задач TaskTracker — однопользовательское SPA",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "hash-password": "node scripts/hash-password.mjs",
    "generate-secret": "node scripts/generate-secret.mjs",
    "import-backup": "node scripts/import-backup.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0",
    "pinia": "^2.3.0",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
